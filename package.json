{
  "name": "quan-ly-thu-chi",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run --prefix frontend build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
